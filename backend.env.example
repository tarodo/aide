# Backend env for Docker Compose (app service)
# Copy to backend.env and adjust values

DATABASE_URL=postgresql+asyncpg://aide:aide@db:5432/aide
JWT_SECRET_KEY=change_me_to_a_random_secret
ENV=dev
LOG_LEVEL=INFO

# Optional: create/ensure initial superuser on `make up`
FIRST_SUPERUSER_EMAIL=admin@example.com
FIRST_SUPERUSER_PASSWORD=changeme
FIRST_SUPERUSER_FULL_NAME=Admin
